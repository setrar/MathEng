{
 "cells": [
  {
   "cell_type": "markdown",
   "id": "ddb1e79e-45b7-4cab-a246-b25df970a077",
   "metadata": {},
   "source": [
    "# Imaginary Numbers\n",
    "\n",
    "## $1 + i = \\sqrt{2} \\cos 45^\\circ + i \\sqrt{2} \\sin 45^\\circ = \\sqrt{2} \\, e^{i (45^\\circ)}$ \n",
    "\n",
    "### **Step 1: Understanding the Complex Number**\n",
    "The number **$1 + i$** is a complex number, which means it has:  \n",
    "- **Real part:** $1$  \n",
    "- **Imaginary part:** $i$, which is equivalent to $1i$  \n",
    "\n",
    "We can represent complex numbers in two common forms:  \n",
    "1. **Rectangular form**: $x + iy$ (like $1 + i$)  \n",
    "2. **Polar form**: $r (\\cos \\theta + i \\sin \\theta)$, where:\n",
    "   - $r$ is the **magnitude** (or length)\n",
    "   - $\\theta$ is the **angle** (or direction)\n",
    "\n",
    "### **Step 2: Find the Magnitude $r$**\n",
    "The magnitude (also called modulus) of a complex number $x + iy$ is:\n",
    "\n",
    "$r = |z| = \\sqrt{x^2 + y^2}$\n",
    "\n",
    "For $z = 1 + i$:\n",
    "\n",
    "$r = \\sqrt{x^2 + y^2} = \\sqrt{1^2 + 1^2}$\n",
    "\n",
    "$\\boxed{ r = \\sqrt{1 + 1} = \\sqrt{2} }$\n",
    "\n",
    "### **Step 3: Find the Angle $\\theta$**\n",
    "The angle $\\theta$ (also called argument) is found using:\n",
    "$\\theta = \\tan^{-1} \\left(\\frac{y}{x}\\right)$\n",
    "\n",
    "For $z = 1 + i$:\n",
    "\n",
    "$\\theta = \\tan^{-1} \\left(\\frac{y}{x}\\right) = \\tan^{-1} \\left(\\frac{1}{1}\\right)$\n",
    "\n",
    "$\\boxed{ \\theta  = \\tan^{-1} (1) = 45^\\circ }$\n",
    "\n",
    "\n",
    "### **Step 4: Express in Polar Form**\n",
    "From complex number theory, we can express any complex number as:\n",
    "\n",
    "$z = r (\\cos \\theta + i \\sin \\theta)$\n",
    "\n",
    "Substituting $r = \\sqrt{2}$ and $\\theta = 45^\\circ$:\n",
    "\n",
    "$\\boxed{ 1 + i = \\sqrt{2} (\\cos 45^\\circ + i \\sin 45^\\circ) = \\sqrt{2} \\, e^{i (45^\\circ)} }$\n",
    "\n",
    "### **Final Answer**\n",
    "This shows that $1 + i$ can be rewritten in polar form using the angle and magnitude."
   ]
  },
  {
   "cell_type": "markdown",
   "id": "20ed39ca-473f-40ad-ab33-78d224c56bd6",
   "metadata": {},
   "source": [
    "## $1 + 1.5i = \\sqrt{3.25} \\left( \\cos 56.3^\\circ + i \\sin 56.3^\\circ \\right)  = \\sqrt{3.25} \\, e^{i (56.3^\\circ)}$\n",
    "\n",
    "We want to express the complex number:\n",
    "\n",
    "$1 + 1.5i$\n",
    "\n",
    "in polar form, which involves finding its **magnitude** and **angle**.\n",
    "\n",
    "### **Step 1: Compute the Magnitude $r$**\n",
    "The magnitude of a complex number $z = a + bi$ is:\n",
    "\n",
    "$r = |z| = \\sqrt{a^2 + b^2}$\n",
    "\n",
    "For $z = 1 + 1.5i$:\n",
    "\n",
    "$r = \\sqrt{1^2 + (1.5)^2} = \\sqrt{1 + 2.25} = \\sqrt{3.25}$\n",
    "\n",
    "Approximating:\n",
    "\n",
    "$\\sqrt{3.25} \\approx 1.803$\n",
    "\n",
    "### **Step 2: Compute the Angle $\\theta$**\n",
    "The angle $\\theta$ (argument of $z$) is given by:\n",
    "\n",
    "$\\theta = \\tan^{-1} \\left(\\frac{b}{a}\\right)$\n",
    "\n",
    "For $z = 1 + 1.5i$:\n",
    "\n",
    "$\\theta = \\tan^{-1} \\left(\\frac{1.5}{1}\\right) = \\tan^{-1} (1.5)$\n",
    "\n",
    "Approximating:\n",
    "\n",
    "$\\tan^{-1} (1.5) \\approx 56.3^\\circ$\n",
    "\n",
    "or in radians:\n",
    "\n",
    "$\\theta \\approx 0.983 \\text{ rad}$\n",
    "\n",
    "### **Step 3: Express in Cosine Form**\n",
    "The polar form of a complex number is:\n",
    "\n",
    "$z = r (\\cos \\theta + i \\sin \\theta)$\n",
    "\n",
    "Substituting the values:\n",
    "\n",
    "$1 + 1.5i = 1.803 \\left( \\cos 56.3^\\circ + i \\sin 56.3^\\circ \\right)$\n",
    "\n",
    "or in radians:\n",
    "\n",
    "$1 + 1.5i = 1.803 \\left( \\cos 0.983 + i \\sin 0.983 \\right)$\n",
    "\n",
    "### **Final Answer**\n",
    "$\\boxed{ 1 + 1.5i = \\sqrt{3.25} \\left( \\cos 56.3^\\circ + i \\sin 56.3^\\circ \\right)  = \\sqrt{3.25} \\, e^{i (56.3^\\circ)} }$\n"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "e5a02519-d055-4396-ac37-3fe6783f0291",
   "metadata": {},
   "source": [
    "## given $C \\cdot \\frac{dv}{dt} = i$  where $v = A \\cos ( \\omega t + \\theta )$ why $i$ is equal to $i = - \\omega C  A \\sin ( \\omega t + \\theta )$  ?\n",
    "\n",
    "We start with the given equation:\n",
    "\n",
    "$C \\cdot \\frac{dv}{dt} = i$\n",
    "\n",
    "where the voltage is:\n",
    "\n",
    "$v = A \\cos ( \\omega t + \\theta )$\n",
    "\n",
    "### **Step 1: Compute $\\frac{dv}{dt}$**\n",
    "\n",
    "Differentiating $v$ with respect to $t$:\n",
    "\n",
    "$\\frac{dv}{dt} = \\frac{d}{dt} \\left[ A \\cos ( \\omega t + \\theta ) \\right]$\n",
    "\n",
    "Using the derivative of cosine:\n",
    "\n",
    "$\\frac{d}{dt} \\cos(x) = -\\sin(x)$\n",
    "\n",
    "we get:\n",
    "\n",
    "$\\frac{dv}{dt} = -A \\omega \\sin ( \\omega t + \\theta )$\n",
    "\n",
    "### **Step 2: Multiply by $C$**\n",
    "\n",
    "$C \\cdot \\frac{dv}{dt} = C \\left[ - A \\omega \\sin ( \\omega t + \\theta ) \\right]$\n",
    "\n",
    "$i = -\\omega C A \\sin ( \\omega t + \\theta )$\n",
    "\n",
    "---\n",
    "\n",
    "### **Final Answer:**\n",
    "$i = -\\omega C A \\sin ( \\omega t + \\theta )$\n",
    "\n",
    "This result follows directly from differentiating the voltage and applying the given capacitor equation.\n",
    "\n",
    "\n",
    "$\n",
    "v = A \\cos(\\omega t + \\theta)\n",
    "$\n",
    "\n",
    "with its **complex exponential representation** using Euler’s formula:\n",
    "\n",
    "$\n",
    "e^{j(\\omega t + \\theta)} = \\cos(\\omega t + \\theta) + j \\sin(\\omega t + \\theta)\n",
    "$\n",
    "\n",
    "Thus, we can express $v$ as:\n",
    "\n",
    "$\n",
    "v = A e^{j(\\omega t + \\theta)}\n",
    "$\n",
    "\n",
    "where we implicitly assume we will take the **real part** when needed.\n",
    "\n",
    "---\n",
    "\n",
    "### **Why Use This Notation?**\n",
    "1. **Avoids Confusion with Current $i$**  \n",
    "   Since $i$ is often used for current, using $j = \\sqrt{-1}$ (common in electrical engineering) prevents ambiguity.\n",
    "\n",
    "2. **Simplifies Differentiation**  \n",
    "   Differentiation of exponentials is much cleaner than trigonometric differentiation:\n",
    "\n",
    "   $\n",
    "   \\frac{d}{dt} e^{j\\omega t} = j\\omega e^{j\\omega t}\n",
    "   $\n",
    "\n",
    "   Instead of dealing with separate sine and cosine terms, we work with a single exponential function.\n",
    "\n",
    "3. **Useful for Phasor Analysis**  \n",
    "   In AC circuit analysis, we often work with phasors where sinusoidal signals are represented as complex exponentials.\n",
    "\n",
    "---\n",
    "\n",
    "### **Applying to the Capacitor Equation**\n",
    "We have:\n",
    "\n",
    "$\n",
    "C \\frac{dv}{dt} = i\n",
    "$\n",
    "\n",
    "If we use $v = A e^{j(\\omega t + \\theta)}$, then:\n",
    "\n",
    "$\n",
    "\\frac{dv}{dt} = A j\\omega e^{j(\\omega t + \\theta)}\n",
    "$\n",
    "\n",
    "Multiplying by $C$:\n",
    "\n",
    "$\n",
    "i = C j\\omega A e^{j(\\omega t + \\theta)}\n",
    "$\n",
    "\n",
    "Since $e^{j(\\omega t + \\theta)} = \\cos(\\omega t + \\theta) + j \\sin(\\omega t + \\theta)$, we extract the imaginary part:\n",
    "\n",
    "$\n",
    "i = j\\omega C A e^{j(\\omega t + \\theta)}\n",
    "$\n",
    "\n",
    "Taking only the **real part**, we confirm:\n",
    "\n",
    "$\n",
    "i = -\\omega C A \\sin(\\omega t + \\theta)\n",
    "$\n",
    "\n",
    "Thus, using the exponential form makes differentiation and circuit analysis more elegant! 🚀"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "e581285f-deee-427d-948d-8580ad837562",
   "metadata": {},
   "source": [
    "# References\n",
    "\n",
    "- [ ] [Why do Electrical Engineers use imaginary numbers in circuit analysis?](https://www.youtube.com/watch?v=FCNHN7B9iDM&t=604s)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "538a30c7-15c4-4ebe-abe2-e0ac166fa45d",
   "metadata": {},
   "outputs": [],
   "source": []
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Julia 1.11.3",
   "language": "julia",
   "name": "julia-1.11"
  },
  "language_info": {
   "file_extension": ".jl",
   "mimetype": "application/julia",
   "name": "julia",
   "version": "1.11.3"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 5
}
